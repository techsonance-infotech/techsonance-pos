# Application Configuration
NODE_ENV=development
NEXT_PUBLIC_APP_NAME=TechSonance POS
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Features
NEXT_PUBLIC_ENABLE_ANALYTICS=false
NEXT_PUBLIC_ENABLE_NOTIFICATIONS=true

# ============================================
# IMPORTANT: Sensitive Credentials
# ============================================
# Database URL, License Keys, and other secrets are now stored in:
# secrets/secrets.dev.json (for development)
# secrets/secrets.qa.json (for QA/staging)
# secrets/secrets.prod.json (for production)
#
# To set up:
# 1. Copy secrets/secrets.example.json to secrets/secrets.dev.json
# 2. Fill in your actual credentials
# 3. Never commit secrets/*.json files (they are gitignored)
#
# See secrets/README.md for more information
# ============================================
