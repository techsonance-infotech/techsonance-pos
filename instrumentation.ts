export async function register() {
    // if (process.env.NEXT_RUNTIME === 'nodejs') {
    //     const { checkAndRunDueScheduledBackup } = await import('@/app/actions/backup')

    //     // Run immediately on startup to catch missed backups
    //     checkAndRunDueScheduledBackup()

    //     // Check every minute
    //     setInterval(async () => {
    //         await checkAndRunDueScheduledBackup()
    //     }, 60 * 1000)
    // }
}
